# Copy this file to .env and fill values before running locally
PORT=3000
DB_HOST=127.0.0.1
DB_USER=hms_user
DB_PASSWORD=your_password
DB_NAME=HMS
DB_PORT=3306

# Example: if using docker-compose, set DB_HOST=db
# DB_HOST=db
